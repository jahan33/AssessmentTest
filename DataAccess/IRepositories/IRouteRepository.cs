using DataAccess.Models;

namespace DataAccess.IRepositories
{
    // Define a public interface named IRouteRepository that inherits from the generic IRepository interface with Route as the type parameter.
    public interface IRouteRepository : IRepository<Route>
    {
    }
}
