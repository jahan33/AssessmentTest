prerequisite: .net 8 sould be installed
Please update the connection string in the "appsettings.json" file
This application will automatically create the database on server, if database does not exist

This application consist of 3 projects
1. DataAccess layer
2. Business layer (Service)
3. interface( console application ) "TestApp"

